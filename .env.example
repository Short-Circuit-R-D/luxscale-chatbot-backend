# === LLM (Groq) ===
GROQ_API_KEY=
LLM_MODEL=

# === Embeddings (HuggingFace, local) ===
EMBEDDING_MODEL=BAAI/bge-m3
# leave empty to force CPU; e.g. "cpu" | "cuda" | "mps"
EMBEDDING_DEVICE=

# === Qdrant ===
# Primary (e.g. Railway internal). Falls back to QDRANT_URL_LOCALHOST if unreachable.
QDRANT_URL=
QDRANT_URL_LOCALHOST=
QDRANT_COLLECTION=

# === MongoDB ===
MONGO_URI=
MONGO_DB=
MONGO_STANDARDS_COLLECTION=

# === Retrieval ===
TOP_K=

# === Auth (future) ===
# API_KEY=

PORT=